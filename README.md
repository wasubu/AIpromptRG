# AIpromptRG

a single txt file to give to an AI to make gadgets

start by previewing the files above and copy and paste it to your desired AI.



The generated code won't work the first try so expect a lot of debugging.



I don't know why but the AI thinks that OnInit() get's called automatically, either call OnInit() globally or put the stuff inside outside and delete OnInit() 

